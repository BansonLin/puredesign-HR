# DECISIONS — 架構決定紀錄

每個架構決定一行：日期｜決定｜理由。只記「會影響後續實作方向」的決定；純實作細節不記。
新增時附在表尾，不改舊列；推翻舊決定時新增一列並註明「取代 D-xx」。

| # | 日期 | 決定 | 理由 |
|---|---|---|---|
| D-01 | 2026-09-03 | 依套件 A 部分建立 CLAUDE.md 為專案規格單一來源；Prompt 0 只產出 docs/PLAN.md，不建 app/、lib/、supabase/ | 套件流程規定：計畫核准前不寫應用程式碼 |
| D-02 | 2026-09-04 | §12 例外檔清單（核准 PLAN 即一併認可，見 PLAN T01 驗收欄／3.4 K2）：根目錄設定檔（含 `.nvmrc`、`components.json`）、`.github/workflows`、`middleware.ts`、`supabase/config.toml` 為 Next.js／CI／Supabase CLI 必要檔；`app/layout.tsx`（Next 必要根 layout）、`app/globals.css`（Tailwind v4 入口）、`app/page.tsx`（A13 依角色導向的根路徑）位於 `app/` 根目錄，不在 §12 任何子目錄；`app/(auth)/layout.tsx`、`app/(front)/layout.tsx`（route group 共用 layout，Next 慣例；T07 登出鈕所在）位於 route group 根目錄，同樣不在 §12 子目錄；`README.md`（repo 既有檔案，T29 只更新連結） | §12 只列 `app/` 子目錄與 `lib/`、`components/` 等；工具鏈與框架必要檔無處可放，先列清單避免中途觸發 §0 停工 |
| D-03 | 2026-09-04 | 不用 `@eslint/eslintrc`（FlatCompat）。`eslint-config-next@15.5.x` 仍是 eslintrc 式（`extends`），沒有 flat config 輸出（PLAN 5.2 的前提只對 16.x 成立），故 `eslint.config.mjs` 以 `createRequire` 沿 `eslint-config-next` 的依賴樹取其 `@next/eslint-plugin-next`、`@typescript-eslint/*`、`eslint-plugin-react`／`react-hooks`／`jsx-a11y`／`import`，用各套件自帶的 flat config 組出等同 `next/core-web-vitals`＋`next/typescript` 的規則 | 不新增白名單外套件（PLAN 5.2、§0）；不依賴 pnpm 10 已預設關閉的 public-hoist；升到 `eslint-config-next@16` 時改為直接 `import` 其 flat config 並移除此組裝 |
| D-04 | 2026-09-04 | CI 資料庫一律用 supabase CLI 本機堆疊：`db` job 與 `e2e` job 各自在自己的 runner `supabase start → supabase db reset`（步驟重複，不做 composite action），`e2e` 以 `supabase status -o env --override-name` 覆寫三個 Supabase 變數；GitHub secrets 只放 `SEED_PASSWORD`；staging 專供本機開發與 C-3 人工驗收，不進 CI。`supabase/config.toml` 由 `supabase init` 產生，`[db.seed] enabled = false`（seed 走 TS 腳本，PLAN 4.9）。金鑰邊界（`tests/unit/secrets-boundary.test.ts`）：`SUPABASE_SERVICE_ROLE_KEY` 只准出現在 PLAN T03／5.5 的四檔 `lib/db/admin.ts`、`tests/e2e/global-setup.ts`、`.env.example`、`ci.yml`；dotenv 檔只掃 `.env.example`（`.env.local` 為本機祕密、已被 `.gitignore` 忽略且依 §3／`db:seed` 必含真金鑰，不在掃描範圍）。**暫時例外（待 Banson 核准）**：`tests/e2e/rls.spec.ts` 也在允許清單——Playwright 無 `react-server` 條件、無法 import `lib/db/admin.ts`，而 `global-setup.ts` 要到 T27 才存在；T27 應在 `global-setup.ts` 匯出 `createServiceRoleClient()`，`rls.spec.ts` 改 import 該 helper 並從允許清單移除 | PLAN A01／K6：CI 打 staging 會清掉 HR 驗收資料且兩個 PR 互相污染；本機堆疊每次從空庫跑到最新也同時驗證 §13「migrations 可從空庫一路跑到最新」；PLAN 4.5 要求 RLS 以真實 anon／authenticated JWT 驗證，只有 CI 有 GoTrue 可簽 token |
| D-05 | 2026-09-04 | 階段邊界：milestone 到期日當天即視為已過（`stageOf` 用 `>= 0`）：第 31 天進第二階段、第 61 天進第三階段、第 91 天（D90 due_date）起「已滿 90 天」 | PLAN A09 逐字「第 91 天起」，且 `dayNumber(start, start+90)=91`；D30／D60 沿用同一邊界以免三段規則不一致（T05） |
| D-06 | 2026-09-04 | `db:seed` 用 `--env-file-if-exists=.env.local`（非 PLAN 5.9 的 `--env-file=.env.local`）；seed 執行檔 `supabase/seed/seed.ts` 只透過 `lib/db/admin.ts` 取 client，帳號一律走 `auth.admin.getUserById/createUser/updateUserById`（固定 id、`email_confirm=true`），不直插 `auth.users` | Node 22 對缺少的 `--env-file` 直接 exit 9，CI runner 沒有 `.env.local`（變數來自 `$GITHUB_ENV`），T27 的 seed 步驟才能直接 `pnpm db:seed`；本機仍照常載入 `.env.local`，語意不變（T04） |
| D-07 | 2026-09-04 | seed 對 `form_templates`／`form_versions` 採「缺才插入、既有列不改」（不是 4.9.2 表列的 upsert）：template 缺時插入、v1 缺時插入 `published`、`active_version_id` 為 null 時才指向 v1；同 key 不同 id 的人工列則印出衝突並中止。其餘表（settings、departments、profiles、milestones）照 4.9.2 upsert | 重跑 seed 不能把 staging 上 HR 已發布的 v2 退回 v1，也不能把已 archived 的 v1 改回 published（會撞 `form_versions_one_published_idx`）；submissions 永遠綁原版本，v1 內容本就不該被改寫（T04） |
