# DECISIONS — 架構決定紀錄

每個架構決定一行：日期｜決定｜理由。只記「會影響後續實作方向」的決定；純實作細節不記。
新增時附在表尾，不改舊列；推翻舊決定時新增一列並註明「取代 D-xx」。

| # | 日期 | 決定 | 理由 |
|---|---|---|---|
| D-01 | 2026-09-03 | 依套件 A 部分建立 CLAUDE.md 為專案規格單一來源；Prompt 0 只產出 docs/PLAN.md，不建 app/、lib/、supabase/ | 套件流程規定：計畫核准前不寫應用程式碼 |
| D-02 | 2026-09-04 | §12 例外檔清單（核准 PLAN 即一併認可，見 PLAN T01 驗收欄／3.4 K2）：根目錄設定檔（含 `.nvmrc`、`components.json`）、`.github/workflows`、`middleware.ts`、`supabase/config.toml` 為 Next.js／CI／Supabase CLI 必要檔；`app/layout.tsx`（Next 必要根 layout）、`app/globals.css`（Tailwind v4 入口）、`app/page.tsx`（A13 依角色導向的根路徑）、`app/favicon.ico`（Next metadata 慣例檔，scaffold 帶入）位於 `app/` 根目錄，不在 §12 任何子目錄；`app/(auth)/layout.tsx`、`app/(front)/layout.tsx`（route group 共用 layout，Next 慣例；T07 登出鈕所在）位於 route group 根目錄，同樣不在 §12 子目錄；`README.md`（repo 既有檔案，T29 只更新連結） | §12 只列 `app/` 子目錄與 `lib/`、`components/` 等；工具鏈與框架必要檔無處可放，先列清單避免中途觸發 §0 停工 |
| D-03 | 2026-09-04 | 不用 `@eslint/eslintrc`（FlatCompat）。`eslint-config-next@15.5.x` 仍是 eslintrc 式（`extends`），沒有 flat config 輸出（PLAN 5.2 的前提只對 16.x 成立），故 `eslint.config.mjs` 以 `createRequire` 沿 `eslint-config-next` 的依賴樹取其 `@next/eslint-plugin-next`、`@typescript-eslint/*`、`eslint-plugin-react`／`react-hooks`／`jsx-a11y`／`import`，用各套件自帶的 flat config 組出等同 `next/core-web-vitals`＋`next/typescript` 的規則 | 不新增白名單外套件（PLAN 5.2、§0）；不依賴 pnpm 10 已預設關閉的 public-hoist；升到 `eslint-config-next@16` 時改為直接 `import` 其 flat config 並移除此組裝 |
| D-04 | 2026-09-04 | CI 資料庫一律用 supabase CLI 本機堆疊：`db` job 與 `e2e` job 各自在自己的 runner `supabase start → supabase db reset`（步驟重複，不做 composite action），`e2e` 以 `supabase status -o env --override-name` 覆寫三個 Supabase 變數；GitHub secrets 只放 `SEED_PASSWORD`；staging 專供本機開發與 C-3 人工驗收，不進 CI。`supabase/config.toml` 由 `supabase init` 產生，`[db.seed] enabled = false`（seed 走 TS 腳本，PLAN 4.9）。金鑰邊界（`tests/unit/secrets-boundary.test.ts`）：`SUPABASE_SERVICE_ROLE_KEY` 只准出現在 PLAN T03／5.5 的四檔 `lib/db/admin.ts`、`tests/e2e/global-setup.ts`、`.env.example`、`ci.yml`；dotenv 檔只掃 `.env.example`（`.env.local` 為本機祕密、已被 `.gitignore` 忽略且依 §3／`db:seed` 必含真金鑰，不在掃描範圍）。**暫時例外（待 Banson 核准）**：`tests/e2e/rls.spec.ts` 也在允許清單——Playwright 無 `react-server` 條件、無法 import `lib/db/admin.ts`，而 `global-setup.ts` 要到 T27 才存在；T27 應在 `global-setup.ts` 匯出 `createServiceRoleClient()`，`rls.spec.ts` 改 import 該 helper 並從允許清單移除 | PLAN A01／K6：CI 打 staging 會清掉 HR 驗收資料且兩個 PR 互相污染；本機堆疊每次從空庫跑到最新也同時驗證 §13「migrations 可從空庫一路跑到最新」；PLAN 4.5 要求 RLS 以真實 anon／authenticated JWT 驗證，只有 CI 有 GoTrue 可簽 token |
| D-05 | 2026-09-04 | 階段邊界：milestone 到期日當天即視為已過（`stageOf` 用 `>= 0`）：第 31 天進第二階段、第 61 天進第三階段、第 91 天（D90 due_date）起「已滿 90 天」 | PLAN A09 逐字「第 91 天起」，且 `dayNumber(start, start+90)=91`；D30／D60 沿用同一邊界以免三段規則不一致（T05） |
| D-06 | 2026-09-04 | `db:seed` 用 `--env-file-if-exists=.env.local`（非 PLAN 5.9 的 `--env-file=.env.local`）；seed 執行檔 `supabase/seed/seed.ts` 只透過 `lib/db/admin.ts` 取 client，帳號一律走 `auth.admin.getUserById/createUser/updateUserById`（固定 id、`email_confirm=true`），不直插 `auth.users` | Node 22 對缺少的 `--env-file` 直接 exit 9，CI runner 沒有 `.env.local`（變數來自 `$GITHUB_ENV`），T27 的 seed 步驟才能直接 `pnpm db:seed`；本機仍照常載入 `.env.local`，語意不變（T04） |
| D-07 | 2026-09-04 | seed 對 `form_templates`／`form_versions` 採「缺才插入、既有列不改」（不是 4.9.2 表列的 upsert）：template 缺時插入、v1 缺時插入 `published`、`active_version_id` 為 null 時才指向 v1；同 key 不同 id 的人工列則印出衝突並中止。其餘表（settings、departments、profiles、milestones）照 4.9.2 upsert | 重跑 seed 不能把 staging 上 HR 已發布的 v2 退回 v1，也不能把已 archived 的 v1 改回 published（會撞 `form_versions_one_published_idx`）；submissions 永遠綁原版本，v1 內容本就不該被改寫（T04） |
| D-08 | 2026-09-04 | `vitest.config.ts` 以 `oxc.jsx.runtime='automatic'` 取代 PLAN 5.4／T01 寫的 `esbuild.jsx='automatic'`；意圖不變（T13 渲染器測試用自動 JSX runtime，tsconfig 仍為 Next 需要的 `jsx: preserve`） | vitest 4.1 綁 Vite 8（rolldown／oxc），`esbuild` 選項被忽略；不設 `oxc.jsx` 時 oxc 會沿用 tsconfig 的 `preserve`，測試檔的 JSX 不會被轉譯（T01） |
| D-09 | 2026-09-04 | `playwright.config.ts` 的 `webServer` 以 `port` 探測就緒，不用 `url` | Playwright 探測 `url` 會跟隨轉址：`/` 307 到 `/login`，T07 前該頁 404，探測會一直等到 timeout；`port` 只等 socket 可連線（T01） |
| D-10 | 2026-09-04 | `components/ui/*` 來源：`shadcn-ui/ui` tag `shadcn@3.8.5` 的 `new-york-v4` 原檔手動放入，只改 `@/lib/utils` → `@/components/ui/utils`（T01 `aliases.utils`），未經 `pnpm dlx shadcn@3 add` | ui.shadcn.com 被本環境 egress 擋，CLI 取不到 registry；日後 CLI 可用時以 `pnpm dlx shadcn@3 add --overwrite <元件>` 核對差異（T01） |
| D-11 | 2026-09-04 | `lib/db/types.ts` 暫為手寫（依 T02 十個 migration 逐欄對齊，格式仿 `supabase gen types` 輸出），T30 前以 `pnpm db:types` 覆蓋並 diff；`lib/db/admin.ts` 匯出 lazy 單例 `getAdminClient()`（首次呼叫才 `createClient`），不在模組頂層建 client | 本容器無 Docker，`gen types` 需 pg-meta 容器；模組頂層建 client 會在 `next build` 靜態評估頁面時因缺 env 直接 throw（CI 的 build 只有 `.env.example` 佔位值）（T06） |
| D-12 | 2026-09-04 | seed 不寫 `audit_log`（**待 Banson 確認**）；PLAN 4.7「建帳號 Server Action 與 seed 呼叫同一函式並在同一處寫 audit」解讀為：共用的是 `milestonesFor()`，audit 由 Phase 2 的建帳號 Server Action 寫，seed（Phase 1）不寫 | `audit_log.actor_id` not null 且 seed 無登入者（service role 執行，沒有 `profiles.id` 可填）；seed 寫入的列以固定 UUID 可辨識（T04） |
| D-13 | 2026-09-04 | 角色不符的「403 頁」用 Next 15 的 `forbidden()`（`next/navigation`）＋ `app/forbidden.tsx`，`next.config.ts` 開 `experimental.authInterrupts: true`；`app/forbidden.tsx` 列為 §12 例外檔（同 D-02 的 `app/` 根目錄慣例檔）。`requireRole()` 流程：未登入 → `/login`（呼叫端可傳 `next`）；有 session 無 profile → signOut 後 `/login?reason=no_profile`；`status='left'` → signOut 後 `/login?reason=disabled`；`must_change_password` → `/login/change-password`（改密碼頁自己以 `allowPasswordChangePending` 略過）；角色不符 → `forbidden()`。矩陣本體在 `lib/auth/policy.ts`（純函式，無 `server-only`），`guard.ts` re-export；「同部門」＝`department_id` 相等且非 null；目標 `left` 只擋回應／週回饋，讀取與匯出仍可（歷史可見）；`newcomer:read` 對 newcomer 角色一律 false（本人看自己走 `log:read_own`） | PLAN T08 用語「403 頁」且 T26／T27 要驗 HTTP 403；`redirect('/login?reason=forbidden')` 會把已登入者踢回登入頁、回 302 而非 403。`forbidden()` 仍標 experimental，若日後 Next 移除，改為 `throw` ＋ `error.tsx` 一處即可（T08） |
| D-14 | 2026-09-04 | `validatePublish(questions, rules, { templateKey, previousPublished? })` 多一個必填 context（PLAN T10 寫兩參數）：語意槽依前綴歸屬範本（`plan.*`／`result.*`→newcomer_daily、`response.*`→manager_response、`weekly.*`→weekly_feedback，`SLOT_SPECS[slot].template`），發布驗證只要求「本範本」的 system slot 與啟用規則 slot 恰一題、其餘至多一題，綁到別範本的 slot 一律拒絕；`previousPublished` 用於「已發布 key 不得改型別」（停用題也受此約束，A07）。`RulesSettings` 型別放 `lib/rules/constants.ts`（T11 的 `parseRulesSettings` 回傳同形狀） | manager_response v1 沒有 `plan.item1.text`，若 system slot 不分範本，三張 v1 種子不可能同時通過發布驗證；T14 呼叫時本來就知道範本 key，顯式傳入比從 slot 前綴推斷穩定（T10） |
| D-15 | 2026-09-04 | 可見性以 `order` 逐題推導並「連鎖」：被 show_if 隱藏或 `disabled` 的題目，對後面題目的條件一律視為 `null`（A11 真值表），`validateAnswers` 存 null、`bySlot`／`readYesterdayPlan` 讀取時同樣套用（隱藏題即使 jsonb 有殘值也讀成 null，停用題不輸出 slot）；`getAnswer` 對缺 key、非字串、空字串一律回 null。`supabase/seed/fixtures/base.ts` 的 `SeedQuestion` 維持本地型別、不改為 import `Question`（T04 的 `fixtures.test.ts` 以 `s is string` 收窄 slot，改成 `Slot` 聯集會讓該檔 typecheck 失敗）；`forms-publish.test.ts` 改以 `parseQuestions` 與 `readonly Question[]` 指派驗證三張 v1 種子符合 zod schema | §6「條件不成立時不顯示、不驗證、答案存空」；連鎖條件下若下游只看原始 jsonb，隱藏題的殘值會讓下游題跳出（A11 的顧慮）；讀寫同一套推導，規則與渲染器看到的答案一致（T10） |
| D-16 | 2026-09-04 | Auth 流程（T07）：(1) 改密碼規則＝長度 ≥ 8 且同時含英文字母與數字（`lib/auth/password.ts` 純函式，頁面與 Server Action 共用）；「與舊密碼相同」靠 Supabase `updateUser` 回的 `same_password` 錯誤碼判定，不另外呼叫 `signInWithPassword` 驗舊密碼；Supabase 後台密碼最短長度須同步設 8（T09 記 RUNBOOK）。(2) `middleware.ts` 只刷新 session cookie（`getUser()`）與把未登入的 `/me`、`/manager`、`/hr`、`/ceo`、`/admin` 導到 `/login?next=…`（302），不做角色、status、must_change_password 判斷（需 service role 讀 profiles，屬 guard.ts）；`/api` 走 middleware 但不導向；因 `lib/auth/session.ts` 帶 `server-only`＋`next/headers`，middleware 自建 `@supabase/ssr` client 並重複 30 天常數。(3) `homeFor(role)` 放 `lib/auth/home.ts`（純函式）：newcomer→`/me/today`、manager→`/manager`、hr／admin→`/hr`、ceo→`/ceo`；登入成功若帶安全的 `next`（同源絕對路徑、非 `/login`）則優先導 `next`，否則 `homeFor`；已登入者開 `/login` 由登入頁（非 middleware）依 profile 導向。(4) 登入／改密碼頁的錯誤訊息以 `?error=`／`?reason=` query 回傳（Server Action redirect），不用 client component，維持無 JS 也可用 | 改密碼頁必須在 `must_change_password=true` 時仍可進入，guard.ts 已提供 `allowPasswordChangePending`；角色判斷若放 middleware 會讓 service role 進 edge runtime、違反 PLAN 5.5 client 分工；`next` 若不檢查會成為 open redirect；Next `redirect()` 固定 307（D-09），middleware 明示 302 以符合 T07 驗收字面（T07） |
| D-17 | 2026-09-04 | `lib/rules/derived.ts`（T12）四個推導皆純函式、`now` 注入，只 import `lib/time`、`lib/rules/constants`（`RESPONSE_STATUS_NEED_HR`）與型別：(1) `hrInterventionList` 比 PLAN T12 簽名多一個必填 `newcomers`（＝`activeNewcomers()` 母體，A02／A04）：alerts／responses 的新人不在母體（left／sample、`target_user_id` null）一律排除，輸出每列附 `newcomer` 供顯示；(2) `responses` 由呼叫端先以 `bySlot` 解出 `response_status`（本檔不讀 `answers`，維持與 `lib/forms` 解耦），比對 trim 後精確相等（A06）；(3) 「近 7 日」＝台北日曆日、含今天（今天與前 6 天），與 A08(d) 同口徑，常數 `HR_NEED_HELP_WINDOW_DAYS=7` 放本檔，未來的回應（`submitted_at` 晚於 `now`）不列；(4) 邊界：R3 缺交用 `isPastCutoff` 的 `>=`（18:00:00 整點即缺交）、A1 逾時與 late 皆嚴格 `>`（恰 24h 仍 `open`）；`logStatus` 對非 active、`start_date` 空或 `date < start_date` 一律 `n/a`（優先於 `hasLog`）；`responded` 但 `responded_at` 為 null 的不一致列視為準時 `responded` | §7「時間型狀態在讀取時推導」；PLAN A02 要求所有清單以 `activeNewcomers()` 為母體，若不在函式內過濾，離職新人的 open alert 會永遠掛在 HR 介入清單（A04 的顧慮）；`derived.ts` 不 import `lib/forms` 才能與 T10 平行開發且保持底層純函式（T12） |
| D-18 | 2026-09-04 | `validatePublish` 的跨欄位檢查（key 唯一、已發布 key 型別、show_if 指向、slot 基數、規則參數值 ∈ options）以純函式手寫，不用 zod `superRefine`；zod 只負責 `parseQuestions` 的單題形狀 | 發布錯誤要以繁中字串清單一次列出全部原因（§6「列出原因」），手寫可直接 push 訊息，不必把 `ZodError` 的 path／issue 轉成中文，跨欄位規則也用不到 zod 的型別推導（T10 審查） |
| D-19 | 2026-09-04 | v1 `support_detail`（`plan.support.detail`）依 §11 為非必填：`validateAnswers` 對「支援＝需要」且說明空白回 `ok`（`forms-validate.test.ts` 另以自建 `required=true` 的版本驗證必填路徑）；PLAN T10 驗收欄「`support='需要'` 且 detail 空 → errors」以 §11 種子為準暫不適用。是否在新版改為 required 待 HR 決定（HR 可在 `/admin/forms` 草稿改 `required` 後發布，不必改程式） | §11 種子是單元測試 fixture 的單一來源（§0），程式不擅自改題目屬性；`required` 是題目資料而非程式碼（T10 審查） |
| D-20 | 2026-09-04 | 補充 D-14：「已發布 key 不得改型別」只比對目前 published 版（`previousPublished`），不回溯 archived 版；成立前提是編輯器不提供刪題（A07：只能停用），因此每個曾發布的 key 都仍在目前 published 版內 | 若日後允許刪題，須改為聯集所有 `published`／`archived` 版的 key／type，屆時另開一列取代本列（T10 審查） |
| D-21 | 2026-09-04 | Server Component（`app/page.tsx`、`/login` 頁、被頁面呼叫的 `requireRole`）無法寫 cookie，`left`／`no_profile` 的失效 session 不在讀取路徑清除：`guard.ts` 的 `signOut()` 包 try/catch 為 best-effort（redirect 一律發生），登入頁對此類 session 只顯示表單，由下一次成功登入覆蓋 cookie；只有 Server Action 路徑（`login`、`changePassword`）真正清掉 session | Next 15 在 Server Component 呼叫 `cookies().set()` 會 throw（`lib/auth/session.ts` 的 `setAll` 已吞掉）；Supabase 不可達時 signOut 也不得阻斷導向（T07／T08 審查） |
| D-22 | 2026-09-04 | `lib/auth/password.ts`（`PASSWORD_MIN_LENGTH`、`meetsPasswordRule`）為 T07 檔案清單外的新增純函式檔，位於 §12 `lib/auth` | `'use server'` 模組（`change-password/actions.ts`）只能 export async 函式，不能 export 常數或同步函式；改密碼頁（顯示規則）、Server Action（驗證）與單元測試需共用同一份規則（T07 審查） |
| D-23 | 2026-09-04 | `reconcile()` 與 `runRules()` 同放 `lib/rules/run.ts`（PLAN T11 未列獨立檔；A10「重跑走同一函式 `lib/rules/run.ts`」）；型別集中 `lib/rules/types.ts`。狀態機補兩處 A10 未言明的細節：(1) 仍成立且 open 但 `detail` 結構相等（`detailEquals`，忽略鍵順序）→ 列入 `untouched` 而非 `updateDetail`（T11 驗收 (b)「答案不變重提 → 全 untouched」）；(2) `insert` 與 `reopen` 的 `created_at` 一律等於傳入的 `now`（呼叫端傳日誌 `submitted_at`，使 §11「≈16.1h」成立），`close` 的 `closed_at=now`、`closed_by=null`、`closed_reason='resubmitted'`。`reconcile` 對 `existing` 泛型保留原列物件（`alert: E`），落庫端以 id 定位；同一 `rule_key` 若被傳入兩列（DB 唯一鍵下不可能）只處理第一列、其餘 untouched | 純函式不碰 DB，T14 `applyAlertChanges`、HR 重跑與 seed 共用同一狀態機；`untouched` 明確列出可讓落庫端零寫入，避免無意義 update 觸發 `updated_at`／audit（T11） |
| D-24 | 2026-09-04 | `parseRulesSettings` 用 zod `strictObject`：未知規則 key（如 `R9`）與未知 params 欄位一律拒絕，`expect_done`／`unreported`／`status_done` 元素不得為空白，`status_done` 至少一值；錯誤以 `RulesSettingsError`（`reasons[]`，訊息 `rules.<path>：<原因>`）丟出，另匯出 `rulesSettingsErrors()` 供 `/admin/rules`（T24）不丟錯地列出原因。R1／R2 比對一律 trim 後精確相等（A06），`null`／空白 status 永不觸發（A11） | PLAN 4.8「非法時丟錯並列出每條原因、不靜默補預設」；寬鬆物件會讓 `/admin/rules` 的打字錯誤（`enable`）靜默停用規則（T11） |
| D-25 | 2026-09-04 | 表單渲染器（T13）：(1) Server Action 與 `FormRenderer` 的契約固定為 `(prevState, formData) => Promise<FormActionState>`，`FormActionState = { ok, errors?: Record<questionKey, string>, message? }`（型別匯出自 `components/forms/FormRenderer.tsx`），T15／T16／T17 的 Server Action 一律回傳此形狀，額外脈絡（對象日誌、日期）由頁面以 `action.bind(null, …)` 綁定，不經渲染器；(2) FormData 只含目前可見題目：文字／日期／數字／`<select>` 直接帶 `name=key`，`single_select` 由渲染器自己的 `<input type="hidden" name=key>` 帶值，Radix RadioGroup 不設 `name`（其 bubble input 只在 client mount 後掛進表單，SSR 與 FormData 行為不一致），隱藏題不在 FormData 內、經 `validateAnswers` 存 `null`；(3) `FieldProps`、`UserOption`、`describedBy` 放在 `components/forms/FieldError.tsx`（T13 檔案清單內唯一適合的共用檔），六個 field 元件皆為純呈現元件；(4) `useActionState`／`useState` 在 react-dom/server 的 `renderToStaticMarkup` 可直接渲染初始狀態，故不拆純呈現層，`tests/unit/forms-renderer.test.tsx` 直接渲染 `FormRenderer`（副檔名 `.tsx`，非 PLAN T13 寫的 `.test.ts`，因測試需 JSX） | §6「答案存空」、A11；PLAN T13「不用 react-hook-form」「不加依賴、不需 jsdom」；後續三個填寫頁面共用同一份錯誤回傳形狀，錯誤才能顯示在對應題目下方 |
| D-26 | 2026-09-04 | 日誌提交管線（T14）：(1) `prepareDailyLog` 的 `existingToday` 內含該筆全部 `alerts` 列（`existingToday.alerts`，各狀態皆含），不另加參數，簽名維持 PLAN T14 的八個欄位；回傳以 `ok` 判別，成功物件除 PLAN 列的 `log_date`／`answers`／`submitted_at`／`alertPlan` 外另帶 `user_id`、`form_version_id`、`updated_at`、`existing_id`，失敗為 `{ ok:false, errors }`，表單層錯誤放 `errors._form`（`FORM_ERROR_KEY`），Server Action 把它轉成 `FormActionState.message`；(2) 「23:59 可改」不另設時刻比對：`log_date=taipeiDateOf(now)`，`existingToday.log_date` 與之不同即回「已超過可修改時間」，同一天內任何時刻皆可改；(3) `reconcile` 的 `now` 一律為本次提交時刻（首次提交等於 `submitted_at`，重提為重提時刻），新 alert 的 `created_at` 因此等於日誌 `submitted_at`（§11 ≈16.1h），重開的 alert 以重提時刻計時（A10）；(4) 落庫非交易：supabase-js 無交易、Phase 1 不加 DB function（migration 不在 T14 檔案清單），順序固定 submission → alerts；`applyAlertChanges` 的 insert 用 `on conflict (submission_id, rule_key) do nothing`（`ignoreDuplicates`），對帳讀取後才出現的列一律不覆寫，其餘寫入以 `alerts.id` 定位；同人同日的並發 insert 由 partial unique index 擋下（23505），Server Action 回「儲存失敗，請重新整理頁面後再試一次」；(5) `insertDailyLog` 先以自然鍵 `getLogByDate` 查找、有列即丟 `今天的日誌已存在`，`updateDailyLog(id, …)` 只改 `form_version_id`／`answers`／`updated_at`（重提永遠改綁 active 版，§6） | A10 要求「同一交易內讀取—比對—寫入」在 PostgREST 下做不到，需 RPC 才能補上（Phase 2 若要嚴格保證，加一支 `submit_daily_log` DB function 並在 DECISIONS 另列）；把 alerts 掛在 `existingToday` 讓純函式的輸入與「今天那一筆」一一對應，seed（T16）與 HR 重跑可用同一形狀（T14） |
| D-27 | 2026-09-04 | `/me/today`（T15）：`submitDailyLog` 契約不變（回 `FormActionState`），`TodayForm`（client）只在客戶端包一層觀察結果：成功時吞掉渲染器的 `message`、改顯示自己的成功卡（「已儲存今日日誌」＋明日三件事，`plan.top_priority` 加「最重要」標記）；明日三件事以 page 在 Server Action `revalidatePath` 後重新讀取今日列所得的 `savedPlan` 為主（`readYesterdayPlan` 對今日日誌讀 `plan.*` slot，跨版本同樣適用），剛送出的 FormData 經同一函式解出的計畫只作首屏 fallback。區塊一以 slot（`result.item{i}.status`）而非 key 定位昨日項目，兩個區塊標題由 `beforeQuestion` 注入（第一題結算題、第一題非結算且無 show_if 的題），不另做第二個渲染器；`NewcomerHeader` 為純元件，接 page 一次取得的 `today` 自行以 `lib/time` 推導（A09），`AppNav` 為唯一的 client 導覽元件（`usePathname` 判定目前頁） | 頁面不必為了成功畫面另加回傳欄位或 hidden input，T16／T17 的填寫頁可沿用同一模式；以 slot 定位讓 HR 改版題目 key 後區塊一仍成立（§6）；D-25 已規定渲染器契約與 `beforeQuestion` 的用途（T15） |
| D-28 | 2026-09-04 | seed 的 submissions 規劃抽成純函式 `supabase/seed/plan.ts`（`buildSeedPlan({ shiftDays })`；T16 檔案清單外、位於 §12 `supabase/seed`）：日誌走 `prepareDailyLog`（與 `/me/today` 同一管線）算出要寫的列與預期 alerts；`seed.ts` 寫完後以資料庫實際 alerts 逐筆對比 plan（不符即 exit 1），`tests/unit/seed-plan.test.ts` 再把 plan 釘在 `EXPECTED_ALERTS`。`--anchor` 平移：`start_date`／`due_date`／`log_date`／`submitted_at` 等距（台北無 DST，instant 加整日），`week_start`＝`weekStartMonday(taipeiDateOf(平移後 submitted_at))`（週回饋維持「提交當週」，不是把 8/31 平移後取週一），`answers.week_start` 同步。主管回應的自然鍵為 `(template_key, user_id, target_submission_id)`（無唯一索引；撞到 ≥2 筆即中止）；回應把對象日誌的 open alerts 改 responded 由 seed 直接寫 `status`／`responded_at`／`response_submission_id` 三欄（T18 的查詢函式尚未存在，且 `lib/db/queries/alerts.ts` 不在 T16 檔案清單）。seed 仍不寫 audit（D-12） | `seed.ts` 頂層有 `main()`，vitest 無法 import；純函式讓「fixture → 列＋預警＋回應」在沒有 Supabase 的環境也能驗證，且 seed 與規則共用同一狀態機（PLAN T16「seed 與規則永不分岐」）；`week_start` 若只平移再取週一，anchor 非 7 的倍數時週回饋會落到前一週，驗收儀表板看不到（T16） |
| D-29 | 2026-09-04 | `DailyLogUpdate`（`updateDailyLog`）不含 `updated_at`：重提路徑的 `submissions.updated_at` 以 DB trigger `submissions_set_updated_at`（PLAN 4.6，`when old.* is distinct from new.*`）為準，應用不再寫；insert 路徑仍由應用寫 `updated_at = submitted_at`（`insertDailyLog` 預設）；`PreparedDailyLog.updated_at` 保留為 `now`（＝ `reconcile` 用的時刻，供呼叫端參考），不再落庫。`applyAlertChanges` 的 `inserted` 改以 upsert 後 `.select('id')` 實際回傳列數計（`ignoreDuplicates` 下被跳過的列不算） | 同一欄由應用與 trigger 兩處寫會分岐（傳入的 `now` 與 trigger 的 `now()` 不是同一瞬間）；seed 重跑改寫相同內容時 trigger 不觸發、列完全不動，`--verify` 才能成立；`inserted` 若照企圖數計，並發下會誤報（T14 審查） |
| D-30 | 2026-09-04 | `/me/today` 兩個區塊標題的位置由 `TodayForm` 依題目推導：「昨日計畫結算」放在第一個未停用的結算題（slot ∈ `result.item{i}.status`／`reason`）之前，「今日回報與明日計畫」放在第一個未停用、非結算、且無 `show_if` 的題目之前（都沒有時退回第一個非結算題）。已知限制：HR 改版若把結算題與其他題交錯排序、或所有非結算題都帶 `show_if`，標題位置可能不如預期；程式不做第二個渲染器，交由 `/admin/forms` 發布前的即時預覽（T22）確認。「最重要」徽章依 `plan.top_priority` 題目的 options 索引對映（items[i] ↔ options[i]；昨日計畫用前一筆日誌自己版本的 options，明日三件事用 active 版），找不到題目或索引即不顯示 | D-27 以 `beforeQuestion` 注入標題；題目順序與選項文字是表單資料而非程式（§6），標題與徽章只能依 slot／索引推導，不能寫死字面值（T15 審查） |
| D-31 | 2026-09-04 | `lib/metrics/dashboard.ts`（T19）：`buildHrDashboard({ now, data })` 為 `/hr` 與 `/ceo` 唯一的組裝純函式，`data` 為 `activeNewcomers()`／`listLogs()`／`listAlertsWithSubmission()`／`listResponsesForSubmissions()`／`listMilestones()`／`getSettings()` 的列（型別以 `Tables<'…'>` 的最小欄位宣告、泛型回傳頁面自己的列型別；responses 沿 D-17 由頁面先以 `bySlot` 解出 `response_status`）。口徑補 PLAN 未言明處：(1) 「應交」＝母體中 `logStatus ≠ n/a` 者（active、有 `start_date` 且今天 ≥ `start_date`），尚未到職或無到職日者不計，與 R3 同一函式；(2) 「待處理預警」＝母體新人所有 `status=open` 的 alert（含往日），最舊在前，附 A1 狀態（`open`／`overdue`）；一行摘要的「待主管回應」即此數；(3) 「預警 N 筆」＝`log_date=今天` 且 `status ≠ closed` 的 alert，依 `created_at` 排序（responded 亦列，closed 不列）；(4) 節點到期只列 `done_at is null` 者：`due_date ∈ [今天, 今天+7]`（含端點，常數 `MILESTONE_DUE_WINDOW_DAYS=7`）加上 `due_date < 今天` 的逾期未做者，`overdue: true` 並依 `due_date` 排最前，已完成的節點不列（清單是 HR 待辦）；(5) 一行摘要拆兩層：dashboard 只回傳 `summary` 事實（`DailySummaryFacts`），頁面加 `APP_BASE_URL` 呼叫 `lib/metrics/summary.ts` 的 `buildDailySummary`，連結為 `baseUrl` 去尾斜線後加 `/`；R1→進度、R2→卡點放 `ALERT_RULE_LABELS`，未知 rule_key 顯示 key 本身；「預警 0 筆」不帶冒號清單。`lib/metrics/index.ts` 由 T23 建立，本檔不建 | §8 要求 `/hr` 與 `/ceo` 數字一致，只能由同一純函式產生；A02 母體與 R3 的 `n/a` 若不對齊，未到職新人會被算成缺交；A09 逐字「[今天, 今天+7] 含端點、逾期未做排最前」；A13 連結走根路徑避免主管被 `/hr` 擋下；摘要字串與 dashboard 分層讓 Phase 3 LINE 推播只需 `buildDailySummary` 而不必載入儀表板（T19） |
| D-32 | 2026-09-04 | /manager 儀表板（T17）：資料組裝為純函式，放在 `components/dashboard` 的 server-safe 模組（`NewcomerCard.tsx`：`parseDashboardSettings`、`newcomerScope`、`latestLogBefore`、`planItemsOf`、`buildNewcomerCard`；`Timeline.tsx`：`buildTimeline`、`alertDetailLines`；`AlertBadge.tsx`：R1→進度、R2→卡點、A1 狀態→待回應／逾時／已回應／已關閉，`responded_late` 顯示「已回應」），`page.tsx` 只讀取、呼叫、渲染並以一次 `new Date()` 注入 `now`。卡片「今日計畫」＝該新人 `log_date` 嚴格小於今天的最近一筆未刪日誌（§8「來自昨日日誌」），今天已交時仍取前一筆；時間軸只以日誌為列（缺交日不另列，R3 只顯示在頁頂卡片的「今日 已交／缺交／未到時」）。/manager 名單母體：manager→`activeNewcomers({departmentId})`、manager 無部門→空清單並提示、hr／admin→全部，結果一律再過 `canAccessNewcomer`；hr／admin 於 /manager 與 /manager/newcomer/[id]（`canRespond().on_behalf`）頁首顯示「HR 代填模式」。`Timeline` 契約 `{ days, readOnly?, renderAction?(day) }` 供 T18（每列回應鈕）與 T25（`readOnly`）重用；回應區標題用「主管回應」（非 §8 字面「我的回應」），回應者 role ∈ {hr, admin} 標「HR 代填」。settings 兩鍵由 `parseDashboardSettings` 嚴格驗證（`daily_cutoff_time` 非 `HH:mm` 或 `response_threshold_hours` 非數字即 throw，不補預設） | 頁面禁止比較時間、純函式才能用 §11 fixture 與四個假時鐘做單元測試（T17 驗收）；§12 不允許新增目錄，`components/dashboard` 既有檔案是純模組唯一落點；若把「今日計畫」解讀為「最近一筆」，新人已交今日日誌時會把明日計畫誤當今日計畫；同一 `Timeline` 要給 HR 唯讀重用，標題不能綁定「我的」；PLAN 4.8 不准靜默預設（T17） |
| D-33 | 2026-09-04 | `lib/metrics`（T23）口徑補 PLAN A08 未言明處，全部純函式、`now` 注入：(1) 預警母體 `alertPopulation()`＝`status ∈ {open, responded}`、所屬日誌未刪（`listAlertsWithSubmission()` 已排除，列上若帶 `submission.deleted_at` 再擋一次）、且傳入 `profiles` 時排除 `sample` 帳號；`left` 與未知 user 的預警仍計（A02「事實全量計」）；(2) 誤報率以 `alerts.response_submission_id` 對應回應列（`MetricResponse = { id, response_status }`，`response_status` 由呼叫端 `bySlot` 解出、trim 後精確比對 A06），找不到回應列的 responded 預警只進分母；(3) `within24hRate` 固定 `WITHIN_HOURS=24`、含等號 `≤`，分母同 `responseRate`（open 亦計入）；A1 `late` 另以 `alertState` 與 `response_threshold_hours` 計、只出現在 `alertRates().late`；(4) `countedThrough(now, cutoff)`＝過 cutoff 後為今天、否則為昨天，缺交率與近 7 日部門統計共用；缺交率以「(工作日 − 日誌) ÷ 工作日」表示，日誌只計 `log_date ∈ [start_date, countedThrough]`，非工作日的日誌可把缺交壓到 0 但不為負，非 active／無 `start_date`／工作日 0 → `null`；(5) 部門統計母體＝active 且有 `department_id` 的新人（無部門者不入任何列），應交自 `max(start_date, windowStart)` 起算，預警以 `created_at` 的台北日期落在 `[today−6, today]` 計，無新人的部門仍出一列（零、`rate: null`），列序依 `sort_order`、`name`；(6) 新人總覽的階段／下一節點直接用 `lib/time/milestones` 的 `stageOf`／`nextMilestone`（D-05 邊界），無 milestones 列時為 `null`；(7) `lib/metrics/index.ts` re-export `rates`／`department`／`newcomer`／`summary`／`dashboard`，`Ratio = { numerator, denominator, rate: number | null }` 為所有比率的共同形狀（分母 0 → `null`，頁面顯示「—」） | §7 指標定義與 A08(e) 只給分子分母、未定義「回應列如何對應」「今天何時計入」「非工作日日誌」等邊界；若不寫死，`/hr` 與 `/ceo`、指標與部門統計會各自解讀而數字不一致；分母 0 若回 `NaN` 會在儀表板顯示 `NaN%`（T23） |
| D-34 | 2026-09-04 | `/me/history`（T21）：列的組裝為純函式 `buildHistoryRows`（`components/dashboard/HistoryList.tsx`，server-safe 模組，與 D-32 同一落點），輸入只含登入新人自己的列（page 以 `user_id`／`userIds:[me]`／`target_user_id=me` 查詢，§10 列 2），builder 不再依人過濾；`now` 由 page 一次注入。口徑：(1) 每日「日誌摘要」＝該筆日誌自己版本（§6 舊 submissions 綁原版本）經 `resolveVisibility` 後可見且有答案的題目，依 `order` 列 `label：答案`，版本找不到即標「找不到這筆日誌的表單版本」；(2) 預警狀態沿用 `AlertBadge`（R1→進度、R2→卡點；A1 狀態由 `alertState` 推導）；(3) 主管回應以 `response.status`／`response.comment` slot 讀出，回應者 role ∈ {hr, admin} 標「HR 代填」；(4) 週回饋以 `week_start = weekStartMonday(log_date)` 對應，同一週的每一天都顯示該週回饋（§8 逐日列出的字面解讀；種子 Darren 9/2 與 9/3 皆顯示 8/31 那筆），三行以 `weekly.good`／`weekly.improve`／`weekly.next_focus` slot 讀、標籤取該週回饋自己的版本；(5) 無日誌顯示「還沒有日誌」，頁面不放 `NewcomerHeader`（§8 只要求按日期列表）。`ResponderLike` 與 `alertDetailLines` 直接重用 `Timeline.tsx` 的匯出，不另複製 | 頁面禁止比較時間、純函式才能用 §11 fixture 與假時鐘做單元測試（T21 驗收）；摘要若寫死 key 會在 HR 改版後失效，靠版本 label 才符合 §6「顯示歷史時用該版本的 label」；週回饋掛每一天而非只掛週五，新人任一天打開都看得到當週回饋（T21） |
| D-35 | 2026-09-04 | 主管回應（T18）：(1) `on_behalf` Phase 1 不落欄位，`prepareResponse`（`lib/forms/submit.ts`，純函式，內部呼叫 `canRespond`）只回傳 `on_behalf` 供頁面提示，顯示端一律由回應者 `profiles.role ∈ {hr, admin}` 推導「HR 代填」（Timeline／HistoryList 已如此）；(2) Server Action 不信任 client 的 `target_user_id`：以 `target_submission_id` 反查日誌（任何狀態），由純函式確認 `template_key='newcomer_daily'`、未軟刪、`user_id` 等於頁面的新人，`target_user_id` 取自日誌；(3) 回應的自然鍵為 `(user_id, target_submission_id)`（DB 無 unique index，以 select 後 insert／update 實作，與 D-06 同理），同一回應者重送＝更新 `answers`／`form_version_id` 並把 `submitted_at` 改為重送時刻（與日誌重提 D-26／D-29 相反：日誌的 `submitted_at` 驅動 R1 `created_at` 與指標，回應列的 `submitted_at` 只餵 A04「需 HR 協助」7 日視窗與排序，重送改成「需 HR 協助」必須從重送時刻進 HR 介入清單；`alerts.responded_at` 仍為首次回應時刻，不重設）、不同回應者各自一列；自然鍵無 unique index，兩個分頁同時送出可能產生兩列，Server Action 讀取全部列、更新最早一列並 `console.warn`（Phase 2 補 partial unique index `(template_key, user_id, target_submission_id) where template_key='manager_response' and deleted_at is null` 與 23505 提示「已送出過，請重新整理」，多出的列由 /admin/data 軟刪）；(4) 預警狀態變更走 `markAlertsResponded(submissionId, responseId, respondedAt, alertIds?)`（`lib/db/queries/alerts.ts`，單一 UPDATE 限 `status='open'`），Server Action 傳入 `prepareResponse().alertPlan.respond` 的 id，純函式的計畫與 DB 寫入為同一真相、`status='open'` 仍是並行守門；`responded`／`closed` 不動、重送不重設 `responded_at`；(5) 回應表單的讀寫（依 id 取日誌、自然鍵取回應、insert／update）以 PLAN 5.5 允許的 admin client 直接寫在 Server Action，未擴充 `lib/db/queries/submissions.ts`（T18 檔案清單外；T22／T25 若需要 `getLogById` 再抽到查詢層）；(6) ceo 或無 `manager_response` 發布版時時間軸以 `readOnly` 渲染、後者另顯示提示，不渲染回應鈕 | §7「主管回應：提交 manager_response → 該日誌所有 open alerts 改 responded」；§10 列 4「可代填（標註 on_behalf）」；§5 submissions 無 on_behalf 欄位、欄位可加不可刪；§7「response.status 為需 HR 協助時在 HR 清單另列」是無條件規定，A04 視窗若以首次回應時刻起算，7 天後改成「需 HR 協助」的回應永遠不會進 HR 介入清單（T18 審查）；`ownResponseAnswers`（抽屜編輯模式的既有答案）放在 `lib/forms/submit.ts` 而非 page.tsx，page 只讀取、呼叫、渲染 |
| D-36 | 2026-09-04 | `/hr` 儀表板（一）（T20）：三個區塊為純呈現元件（`components/dashboard/TodaySubmissions.tsx`／`AlertList.tsx`／`InterventionList.tsx`，server-safe、無 `use client`），只吃 `buildHrDashboard(...)` 的 `today`／`pendingAlerts`／`intervention` 三個切片，不再自行過濾或比較時間；`page.tsx` 一次 `new Date()`，settings 沿用 `parseDashboardSettings`（D-32，嚴格驗證）再轉成 `DashboardSettings` 兩鍵，不另寫第二份驗證。取數：`listLogs({ userIds, dateTo: today })` 不設起日（回應以 `target_submission_id` 對應日誌，近 7 日的「需 HR 協助」回應可能指向舊日誌，A04）、`listAlertsWithSubmission({ userIds })` 全狀態、`listMilestones({ pendingOnly: true })`；`response.status`／`response.comment` 由頁面依每筆回應自己的版本 `bySlot` 解出（D-17），`response_comment` 只供介入清單顯示主管一句話。預警名單連結：T25 前一律 `defaultNewcomerHref` → `/manager/newcomer/[id]`（hr／admin 依 §10 列 3 可開），`hrefFor` prop 供 T25 換成 `/hr/newcomer/[id]`；規則標籤用 `entry.label`（`alertRuleLabel`，D-31 單一來源），A1 狀態文字重用 `AlertBadge` 的 `ALERT_STATE_LABELS`，R1／R2 細節重用 `Timeline.alertDetailLines`。`CopySummaryButton`（`use client`）：摘要字串由 server `buildDailySummary` 產生後傳入，唯讀文字框永遠顯示（HR 先看再貼，focus 即全選），按鈕先試 `navigator.clipboard.writeText`，不存在或 reject（LINE 內建瀏覽器、非 https）即全選文字框並顯示「無法自動複製，已為您選取文字，請長按複製」，不做 `document.execCommand('copy')`。三區塊皆用清單與 4 欄計數格、不用 `<table>`，375px 不會橫向捲動；HR 儀表板其餘區塊（近 7 日部門統計、三指標、新人總覽、節點到期）為 T23／儀表板（二） | PLAN T20 逐句：`buildHrDashboard(now)` 餵三區塊、T25 前暫連 `/manager/newcomer/[id]`、clipboard 退回可全選唯讀文字框；頁面禁止比較時間、純元件才能用 §11 fixture 與假時鐘做 `renderToStaticMarkup` 單元測試（T20 驗收）；`execCommand` 已棄用且在 React 受控 textarea 下行為不一；日誌不設起日與 /manager（D-32）同一取捨，Phase 1 規模可接受（T20） |
| D-37 | 2026-09-04 | 週回饋（T22）：(1) 純函式 `prepareWeeklyFeedback`／`weeklyInitialAnswers`／`weeklyReminders` 放 `lib/forms/submit.ts`（T22 檔案清單內唯一的純函式落點；`page.tsx` 不能匯出額外函式、`actions.ts` 為 `'use server'` 只能匯出 async），`app/(front)/manager/weekly/WeeklyForm.tsx`（client wrapper，清單外新增檔，與 T15 `TodayForm.tsx`／T18 `ResponseDrawer.tsx` 同一慣例）承載對象下拉與成功卡。(2) 權限：頁面母體同 /manager（`newcomerScope` → `activeNewcomers` → `canAccessNewcomer`），再以 `canRespond().allowed` 過濾；Server Action 以 `requireRole(['manager','hr','admin'])` → 依 id 反查 profile 並 `canAccessNewcomer`（否則 403）→ 純函式內 `canRespond`（§10 列 4「填週回饋」，left 對象拒絕、給 `on_behalf`）；`on_behalf` 沿 D-35 不落欄位。(3) 對象下拉不在表單 FormData 內：選擇即導向 `/manager/weekly?newcomer={id}`，頁面以 `submitWeeklyFeedback.bind(null, { newcomerId })` 綁定（D-25），Action 不信任 client 傳的對象；唯一一位新人時自動選取，未選取時送出鈕停用。(4) `week_start`＝slot `weekly.start_date` 的答案經 `weekStartMonday` 正規化（任一天 → 該週一）並寫回該題答案（「答案同步存」以 slot 綁定的 key，不寫死 `week_start`）；該 slot 無題目或答案為空時退回 `now` 的台北當週；slot 題若非 `date` 型別另以 `isDateString` 把關。自然鍵 `(user_id, target_user_id, week_start)` 以 select 後 update／insert 實作（D-06），更新時 `submitted_at` 改為重送時刻（同 D-35 的回應列；週回饋的 `submitted_at` 只供排序，無指標依賴），並發重複由 partial unique index 擋下並回「送出失敗，請重新整理」。(5) 週五提醒：`weeklyReminders({ today, newcomerIds, feedback })` 只在台北週五（`isFriday`）成立，「本週尚無週回饋」以任何作者（主管或 HR 代填）對該新人、`week_start = weekStartMonday(today)` 的未刪列判定；/manager 每天都查本週列、由純函式決定是否顯示；提醒以卡片下方連結列呈現（`NewcomerCard.tsx` 不在 T22 清單，不改其 props），連到 `/manager/weekly?newcomer={id}` | §8「/manager/weekly 依 weekly_feedback 表單渲染」「週五顯示週回饋未填提醒」；§5 partial unique 只認未刪列且不能當 upsert 目標；頁面禁止比較時間，只有純函式才能以 §11 fixture（工務主任對 Darren 8/31 週已填 → 9/4 週五不提醒、其餘三人提醒）與假日期做單元測試；若提醒只看「本人」的回饋，HR 代填後主管仍會被提醒（T22） |
| D-38 | 2026-09-04 | `/hr` 儀表板（二）（T24）：四個新區塊同 D-36 為純呈現元件（`components/dashboard/MetricsTiles.tsx`／`DepartmentStats.tsx`／`NewcomerOverview.tsx`／`MilestoneDue.tsx`，server-safe、無 `use client`），只吃 `alertRates()`／`departmentStats7d()`／`newcomerOverview()`／`buildHrDashboard(...).milestones` 的結果；顯示用純函式（`formatPercent`（`rate===null` → 「—」、四捨五入至小數一位）、`ratioLabel`、`metricsFootnote`、`windowLabel`、`dayLabel`、`nextMilestoneLabel`、`milestoneDueLabel`）與元件同檔匯出並以 §11 fixture 單元測試；兩張表放 shadcn `Table` 自帶的 `overflow-x-auto` 容器，其餘區塊不用表格，頁面本體 375px 不橫向捲動 | §8「近 7 日各部門統計、三指標、新人總覽、節點到期清單」；§0「頁面數字一律來自純函式」；D-32／D-36 已把 server-safe 純函式落在 `components/dashboard`，T24 沿用同一落點；`/ceo`（T26）要重用同一批元件，格式化留在元件內才不會兩頁不一致（T24） |
| D-39 | 2026-09-04 | T24 檔案清單外的三處必要新增／調整：(1) 新增 `lib/db/queries/departments.ts`（`listDepartments()`）——`departments.name` 只有這張表有，近 7 日各部門統計與新人總覽的「部門」欄都需要，頁面不得自行 `getAdminClient()` 查詢（§12 查詢只落 `lib/db`）；(2) `settings.workweek` 的驗證 `parseWorkweekSetting()` 放 `components/dashboard/DepartmentStats.tsx`（與 `parseDashboardSettings` 同精神，讀不到合法值即擲錯，不靜默補 `mon_fri`）；(3) `/hr` 的 `listMilestones()` 不再帶 `pendingOnly`，改讀全部節點——新人總覽的「階段」需三筆齊全（`stageOf` 缺任一筆回 null），節點到期清單自己會濾掉 `done_at` | §8／§12；PLAN A08(d)（應交依 workweek）、A09（階段以 milestones 推導）；若 `pendingOnly` 留著，D30 一完成整欄階段就變「—」（T24） |
| D-40 | 2026-09-04 | 收掉 T20 三處 review minor：(a) `/hr` 的 `APP_BASE_URL` 未設定即擲錯「APP_BASE_URL 未設定」，不再 `?? ""` 靜默退回（否則一行摘要的連結變成貼到 LINE 沒人打得開的「/」）；(b) `alertRates()` 一律傳入 `listProfiles()`，`sample` 帳號的預警在三指標永遠排除（A02），不倚賴「預警查詢已限定 `activeNewcomers()`」這個間接保證；(c) HR 介入清單「需 HR 協助」列依回應者角色顯示「HR 代填於 …」／「主管回應於 …」（`responderPrefix()`，角色由頁面以 `listProfiles()` 解出），與 Timeline／HistoryList 的「HR 代填」標記一致 | §4 `APP_BASE_URL`；PLAN 4.8「不靜默補預設」；A02 sample 不進指標；D-35 on_behalf 由回應者 role 推導；`page.tsx` 無法在 Vitest 匯入（`server-only`），故 (a) 以 `summaryLink('')==='/'` 與 `buildDailySummary` 的說明性斷言記錄其危害（T24） |
| D-41 | 2026-09-04 | `/hr/newcomer/[id]` 與該員 CSV 匯出（T25）：(1) 頁面 `requireRole(['hr','admin'])` 後再 `requireNewcomerAccess(id)`（後者＝`requireRole(['manager','hr','ceo','admin'])`＋`canAccessNewcomer`），前者把主管與 ceo 擋在 HR 視角外，後者確認對象是新人並套 §10 列 3；時間軸重用 T17 `Timeline` 的 `readOnly`（不傳 `renderAction`），HR 要回應仍走 `/manager/newcomer/[id]`（D-35 代填），節點紀錄表單為 Phase 3。(2) Route Handler `app/api/export/newcomer/[id]/route.ts` 先 `getSessionUser()`，未登入直接回 401 純文字（middleware 的 `PROTECTED_PREFIXES` 不含 `/api`，且下載端點不該回 HTML 登入頁），有 session 才呼叫 `requireNewcomerAccess`；其 `forbidden()` 在 Route Handler 由 Next 的 app-route module 轉成 403（newcomer、跨部門主管），採購主管對嚴雅齡 200。(3) `lib/db/csv.ts` 為純模組（不 import `server-only`／`lib/db/admin`，`tests/unit/csv.test.ts` 可直接 import）：cell 只有含 `,`／`"`／CR／LF 才加引號、內部 `"` 加倍、`null` 為空格；BOM＋CRLF 且尾端補一個 CRLF；`filename` 把 `[A-Za-z0-9._-]` 以外字元換成 `_`。(4) CSV 口徑補 PLAN 未言明處：題目欄以「該員日誌用到的版本」依 `version_no` 遞增、版本內依 `order` 首次出現決定欄序（新版只會往後追加欄，兩次匯出可對照），表頭 label 取最新含該 key 的版本；`disabled` 題保留欄位（答案為 null，A07）；資料列依 `log_date` 遞增（最舊在前）；`submitted_at` 以 `formatTaipei` 輸出 `yyyy-MM-dd HH:mm`；`alerts` 含全部狀態（含 closed）依 `rule_key` 排序以 `;` 串接；一筆日誌有多筆回應時 `response_status`／`response_comment` 取 `submitted_at` 最新的一筆。(5) `buildNinetyDayOverview`（`components/dashboard/NinetyDayOverview.tsx`）以 `newcomerOverview` 為主，但因 §10 列 3 允許 HR 讀已離職新人、而該函式母體只有 active（A02／A08(c)），非 active 時以同樣的 `lib/time`／`alertPopulation` 就地推導，`missingRate` 為 `null`；百分比與分數字串重用 T24 的 `formatPercent`／`ratioLabel`，不另寫一份。(6) T25 檔案清單外多一個測試檔 `tests/unit/ninety-day-overview.test.tsx`（頁面數字必須以 §11 fixture 與假時鐘測試，`csv.test.ts` 的檔名不涵蓋總覽）；`/hr` 的 `AlertList`／`InterventionList` 仍用 `defaultNewcomerHref`（D-36 的 `hrefFor` 尚未切到 `/hr/newcomer`），因 `app/(front)/hr/page.tsx` 不在 T25 檔案清單，留待維護者一行 prop 切換 | §8「/hr/newcomer/[id]：90 天總覽、時間軸、匯出該員 CSV」與 §10 列 9；PLAN T25 逐句（401／403／200 四個案例、BOM／CRLF／Content-Disposition、`buildNewcomerCsvRows` 供測試）；`requireRole` 對未登入是 `redirect()`（302/307），API 需要狀態碼而非導向；CSV 欄位若以「最新版本的 order」排序，HR 每次改版都會讓欄序跳動、無法與前一次匯出對照（T25） |
| D-42 | 2026-09-04 | 時間戳排序一律走 `lib/time` 的 `toInstant()`（`components/dashboard/Timeline.tsx` 的主管回應列、`components/dashboard/HistoryList.tsx` 的主管回應列與週回饋列一律改用同名的 `compareInstant`；兩檔的 `compareIso` 只留給 `log_date`／`rule_key`，並在定義處註明），字典序比較只留給 `log_date`／`rule_key` 這類非時間字串 | `submitted_at` 是 timestamptz，同一瞬間可能以 `+08:00` 或 `Z` 兩種寫法出現（seed 寫 `+08:00`、PostgREST 回 `+00:00`），字串比較會把 `09:10+08:00` 排在 `01:10Z` 之後而反轉順序 |
| D-43 | 2026-09-04 | `AppNav` 的 current 改為「最長匹配的 href 唯一 current」（純函式 `currentNavHref(pathname, items)`，單元測試 `tests/unit/app-nav.test.tsx` 只測該純函式，因元件用 `usePathname()` 需 app-router context，無 jsdom 無法渲染） | 導覽項互相包含（`/manager` 是 `/manager/weekly` 的前綴），前綴比對會讓 `/manager/weekly` 同時有兩個 `aria-current="page"`，違反 ARIA 與螢幕閱讀器預期 |
| D-44 | 2026-09-04 | R1→進度／R2→卡點 的字面值只留 `lib/metrics/summary.ts` 的 `ALERT_RULE_LABELS`（A13）一處；`components/dashboard/AlertBadge.tsx` 改為 re-export（`ALERT_KIND_LABELS = ALERT_RULE_LABELS`、`alertKindLabel = alertRuleLabel`），呼叫端名稱不變 | 同一組標籤原本在儀表板摘要與時間軸徽章各寫一份，改一邊就會與 LINE 一行摘要不一致；`summary.ts` 只 import `lib/time`，不觸發 secrets-boundary |
| D-45 | 2026-09-04 | `/me/history` 同一週的週回饋只顯示在該週最新一列（`HistoryRow.showWeekly`，由新到舊掃描時記錄已顯示的 `week_start`），其餘列整個週回饋區塊不渲染 | 一筆週回饋原本在該週每一列重複出現，新人往下捲看到三、四次同樣三行；被抑制的列若只是清空清單會顯示「本週尚無週回饋」而誤導，故整區不渲染 |
| D-46 | 2026-09-04 | 日誌表單版本讀不到時區分兩種原因：版本列不存在＝「找不到這筆日誌的表單版本」，版本存在但 `questions` jsonb 解析失敗＝「表單版本內容無法解析」（`buildHistoryRows` 多收 `unparseableVersionIds`，`HistoryRow.versionError: 'missing' | 'unparseable'`） | 頁面的 `loadVersions` 對兩種情況都只是「不放進 map」，HR／新人看到「找不到版本」會去找不存在的資料庫問題；解析失敗要指向 `form_versions.questions` 內容（Phase 2 表單編輯器發布驗證的線索） |
| D-47 | 2026-09-04 | `/manager/weekly` 的送出成功卡以 `key={selected?.id ?? ""}` 讓 `WeeklyForm` 隨對象切換 remount 重置（不在 `onSelect` 手動 `setSaved(false)`） | 成功卡是 client 端 `useState`，換對象不重置會讓主管以為已為新對象送出；重置行為發生在 page.tsx 的 React 協調，Phase 1 無 jsdom 無法以單元測試涵蓋，`tests/unit/weekly-form.test.tsx` 只釘初始狀態、remount 行為留給 e2e（T27） |
| D-48 | 2026-09-04 | `/ceo`（T26）與 `/hr` 共用同一組區塊與同一組純函式（`buildHrDashboard`／`alertRates`／`departmentStats7d`／`newcomerOverview`），差別只在 props：不渲染 `CopySummaryButton`（因此 `/ceo` 不讀 `APP_BASE_URL`），並對 `AlertList`／`InterventionList`／`NewcomerOverview` 一律傳 `hrefFor={null}`（三個元件的 `hrefFor` 型別擴為 `((id)=>string) | null`，`null` 時姓名渲染成 `<span>` 純文字；`/hr` 的預設值與行為不變），使 `main` 內 `button`／`form` 數為 0、且無任何 `/hr/newcomer` 或 `/manager/newcomer` 連結。`requireRole(['ceo'])` 刻意不含 admin（admin 的家是 `/hr`，`homeFor('admin')==='/hr'`），故 admin 開 `/ceo` 為 403。頁面層級常數不得以 `export` 宣告（Next 15 對 `page.tsx` 的匯出白名單會讓 `next build` 失敗：「… is not a valid Page export field」），`READ_ONLY_LABEL` 因此為模組內 `const` | §8「/ceo：與 /hr 相同唯讀，僅儀表板與新人總覽，無操作按鈕」與「數字必然一致」；§10 admin 為 hr 超集、ceo 唯讀；T26「main 內 button／form 元素數 0」；登出鈕在 `app/(front)/layout.tsx` 的 header，不在 `main` 內，故不需為 `/ceo` 另做 layout 例外（T26） |
| D-49 | 2026-09-04 | CSV 匯出防公式注入：`lib/db/csv.ts` 的 `csvCell()` 對以 `=` `+` `-` `@` `\t` `\r` 開頭的儲存格先前置一個單引號（`CSV_FORMULA_GUARD`），再套既有 RFC 4180 引號規則；對稱的 `unescapeCsvFormulaGuard()` 同檔匯出，**Phase 2 的 CSV 匯入必須先呼叫它還原**，否則搬進 `answers` 的值會多一個單引號。還原只在「去掉開頭單引號後仍以那些字元開頭」時發生，因此真的以單引號開頭的答案（「'今天很順」）不受影響；唯一不可逆的形狀是「'=1+1」（單引號後緊接公式字元），種子與 §11 都沒有這種答案，選擇不為它加第二層跳脫以免 HR 在 Excel 看到多餘字元 | HR 用 Excel／Numbers 開匯出檔，新人自由文字若以 `=`／`+`／`-`／`@` 開頭會被當公式執行（`-5`、`+886…`、`@小明` 都是真實會出現的答案）；匯出與匯入是同一組人同一份檔案，防護與還原必須成對存在才可逆（Layer 5 審查裁決） |
| D-50 | 2026-09-04 | `app/api/export/newcomer/[id]/route.ts` 改呼叫 `newcomerCsv({...})`，不再自行 `buildNewcomerCsvRows()` + `toCsv()`；檔頭原本誤標的 `(D-38)` 更正為 `(D-41 (2))` | 手抄的兩步組裝讓 `tests/unit/csv.test.ts` 對 `newcomerCsv` 的覆蓋測不到 production 路徑，D-49 這類跨兩個函式的改動會漏；行為不變（`newcomerCsv` 就是那兩步）（Layer 5 審查 minor） |
| D-51 | 2026-09-04 | `AlertSubmission`（`lib/db/queries/alerts.ts`）型別與 select 字串補 `deleted_at`，`/hr`、`/hr/newcomer/[id]`、`/ceo` 三頁刪掉把 `submission.deleted_at` 硬寫成 `null` 的 map，改把 `listAlertsWithSubmission()` 的列直接交給 `lib/metrics`；`alertPopulation()` 的第二道防線因此擋的是真值 | 原本頁面自己斷言 `deleted_at: null`，等於把 A05 (1) 的保證抄成常數：查詢若哪天漏掉 `.is('submissions.deleted_at', null)`，三指標也不會發現。多選一個欄位的成本遠低於這個風險（Layer 5 審查 minor） |
| D-52 | 2026-09-04 | `tests/unit/ceo-dashboard.test.tsx` 的 `CeoBlocks`／`HrBlocks`／`viewAt()` 是 `app/(front)/ceo/page.tsx`（與 `/hr`）區塊組合與指標呼叫的人工複本（Vitest 不能 import `server-only` 頁面），檔頭明寫「兩處必須同步修改」；`viewAt()` 的 `alertRates` 補 `profiles` 與頁面一致，並加一條數值斷言釘住 9/4 18:00 的誤報率 0%／主管回應率 50%／24h 內回應率 50%／今日交件 已交 0 缺交 4，且 `/hr` 與 `/ceo` 三指標逐格相等 | 複本會靜默過期：頁面改了而測試沒改時，測試仍全綠但測的是不存在的儀表板；§8 要求 `/ceo` 與 `/hr` 數字一致，只有把數字釘住才測得到「一致」而非只測到「都有渲染」（Layer 5 審查 minor） |
| D-53 | 2026-09-04 | `components/dashboard/AppNav.tsx` 的 current 判定改以索引唯一化（`items.findIndex((c) => c.href === currentHref) === index`），`<li>` 的 key 一併改為 `${href}-${index}`；`currentNavHref()` 行為不變（回傳最長匹配的 href） | 原 JSDoc 寫「同 href 出現兩次保留第一個」但實作以 `item.href === currentHref` 比對，兩個重複項會同時是 `aria-current`（也會撞 React key）；文件與實作二選一時選實作對齊文件（Layer 5 審查 minor） |
| D-54 | 2026-09-04 | `NinetyDayOverview` 缺交率那格的 hint 改為「缺 N / M 個工作日（計至 yyyy/MM/dd）」（`missingRateHint()`，日期取 `MissingRate.countedThrough`，格式與頁面其他日期同為 `formatDate`） | 同一組四格裡「累計日誌」算到今天、缺交率只算到 `countedThrough`（未過 cutoff 的今天不計，D-33），不標截止日會被讀成兩個口徑打架（Layer 5 審查 minor） |
| D-55 | 2026-09-04 | `NewcomerOverview` 的「下一節點」欄以 `row.stage !== null` 當作「這位新人有節點資料」，維持現狀：節點列殘缺（例如只有 D60、D90）時該欄顯示「尚未建立節點」而非「已完成全部節點」 | 節點是新人到職時一次建三筆（§5），殘缺只可能來自手動改庫；此時顯示「尚未建立節點」會讓 HR 去 `/admin` 補資料，比顯示「已完成」誤導安全（Layer 5 審查 minor，記錄而不改） |
| D-56 | 2026-09-04 | 待辦（Phase 2）：`compareInstant`（以 `toInstant()` 比較 timestamptz，D-42）目前在 `components/dashboard/Timeline.tsx`、`components/dashboard/HistoryList.tsx`、`lib/db/csv.ts` 各有一份，Phase 2 抽到 `lib/time` 單一來源 | 三份同名同義的比較器，改一份就會與另兩份不一致；Phase 1 不動是因為抽出會同時碰三個已審過的檔案（Layer 5 審查 minor） |
| D-57 | 2026-09-04 | `tests/e2e/global-setup.ts` 匯出 `createServiceRoleClient()`／`hasSupabaseEnv()`，成為 `tests/e2e` 唯一可讀 service role 金鑰的檔案；`rls.spec.ts` 改 import 它、不再自建 client，`secrets-boundary.test.ts` 的允許清單回到 PLAN T03／5.5 的四檔（`lib/db/admin.ts`、`tests/e2e/global-setup.ts`、`.env.example`、`ci.yml`），D-04 的暫時例外結束 | Playwright 無 `react-server` 條件、不能 import 含 `server-only` 的 `lib/db/admin.ts`；金鑰入口收斂成一個檔案後，e2e 新增 spec 不必再擴充允許清單（T27） |
| D-58 | 2026-09-04 | e2e 在缺少 Supabase 環境變數時：`CI=true` 一律拋錯中止（globalSetup），非 CI 則印警告、globalSetup 跳過重設與 seed、各 spec 以 `test.skip(!hasSupabaseEnv())` 跳過 | 本機無 Docker 就沒有 PostgREST／GoTrue（PLAN A01「本機無 Docker 時 e2e 只在 CI 跑」），但同一個 skip 若發生在 CI 會讓 e2e job 全綠卻什麼都沒驗；兩種環境必須有不同結局（T27） |
| D-59 | 2026-09-04 | `flow.spec.ts` 走 R2（卡點）而非 R1，且四位新人今日日誌由 globalSetup 以 service role 先刪再 `pnpm db:seed`；三個角色在同一個 test 內以登出／登入串接（不開多個 context），主管卡片斷言 open 預警數＝2、不斷言逾時數 | R1 需要「昨日預計完成」才成立，會隨執行日期漂移；R2 只看今日答案，任何日期都成立。重跑同一天必須從空白今日日誌開始，否則按鈕變「更新今日日誌」、預警已被回應。逾時數取決於與 §11 fixture（2026-09-03）的距離，故不入斷言（T27） |
| D-60 | 2026-09-04 | seed 的三道防線（PLAN 核准項 (3) 的兩個新環境變數）：(1) `SEED_PASSWORD` 未設或空白時每個模式（含 `--milestones-only`）都 `abort`；(2) `SEED_ALLOWED_PROJECT_REF` 必須等於 `NEXT_PUBLIC_SUPABASE_URL` 的 `<ref>`（supabase CLI 本機堆疊為 `local`），不符即中止；(3) 完整模式（含 fixture）在 `NODE_ENV=production` 拒絕執行，`--anchor` 只准與完整模式並用且在 `CI=true` 時拒絕。production 只准 `--base` 與 `--milestones-only` | 示範日誌灌進 production、或把 staging 的示範資料寫到別的專案，都是不可逆的；`.env.local` 指錯專案是最可能的人為失誤，故不靠人記得而靠 ref 比對擋（PLAN 4.9.2、A01；RUNBOOK 1.1／1.2） |
| D-61 | 2026-09-04 | seed 帳號組成與密碼策略（A01／A03 的落地）：base 三個帳號 `banson`(admin)／`hr`(hr)／`ceo`(ceo，§11 未列但 C-3 驗收要用)；fixture 另加四主管、四新人與 `e2e_fresh`（`status='sample'`、`must_change_password=true`）。所有示範帳號明確寫 `must_change_password=false`（`profiles.default true` 只適用 HR 後台新建的帳號）。`ensureAuthUser` 對已存在的 auth 使用者預設**不動密碼**，只有 `--reset-passwords` 或帳號是 `e2e_fresh` 時才 `updateUserById`；`profiles` 每次都 upsert 整列，因此 `e2e_fresh` 的 `must_change_password` 每次重跑都回到 `true` | 若每個示範帳號都要走首次改密碼，煙霧測試第二次就登不進去（改過密碼了）；`e2e_fresh` 反過來必須每次回到「未改密碼」才測得到強制改密碼流程。密碼不重設是為了讓 HR 在 staging 上自己改過的密碼不會被下一次 seed 洗掉（RUNBOOK 1.3／6.3） |
| D-62 | 2026-09-04 | `alerts.created_at` 一律等於觸發它的那筆日誌的 `submitted_at`（`runRules`／`reconcile` 的 `at`），不是資料庫的 `now()`：新建與「closed 後再成立」的 reopen 都用它，`--anchor` 平移後也跟著平移 | A1 的 24 小時時鐘必須從「新人交件的那一刻」起算，用 `now()` 會讓 seed／重跑／匯入的歷史資料時鐘全部從跑指令的當下起算，§11「responded_at − created_at ≈ 16.1h」與 9/4 18:00 的逾時判定就對不上（PLAN T16、A10） |
| D-63 | 2026-09-04 | 主管可以對「沒有預警」的日誌送出 `manager_response`：`prepareResponse` 不要求 `alerts` 非空，抽屜顯示「這天沒有預警，仍可留下一句話給新人。」，alertPlan 的 `respond` 為空陣列。這類回應仍會進「需 HR 協助」清單（若狀態選它），但不進三指標的任何分子分母 | §11 種子就有這個案例（工務主任 9/4 回應 Darren 9/3，該日誌零預警）；§7 誤報率明言只以預警為母體，所以「無預警日誌的回應」不能算誤報，否則指標會被主管的日常留言稀釋（PLAN T18、A08） |
| D-64 | 2026-09-04 | 30 天 session 的作法＝`lib/auth/session.ts` 的 cookie `maxAge = 60*60*24*30` ＋ Supabase refresh token rotation（access token 仍短期，由 `middleware.ts` 每次請求刷新；middleware 因 `session.ts` 帶 `server-only` 而重複同一常數）。**「連續 30 天不失效」本身未實測**，只以「把 staging JWT expiry 暫調 5 分鐘 → 閒置 10 分鐘後仍在線 → 調回」驗證刷新機制有效；真正的 30 天列入上線後檢討，Supabase 專案端的 session 上限若設得比 30 天短會提前失效（RUNBOOK 8.1） | 等 30 天才能驗收不可行，但「刷新有效」與「30 天有效」是兩件事，必須寫清楚哪一件已驗、哪一件沒驗，避免日後把未驗證項當成已驗證（PLAN T09） |
| D-65 | 2026-09-04 | 三筆 milestones 由**應用層**建立：純函式 `milestonesFor(start_date)`（`lib/time/milestones.ts`）產生 D30／D60／D90，seed 與（Phase 2 的）建帳號 Server Action 共用同一函式；不用 DB trigger、不用 generated column。`milestones` 的 `unique(user_id, kind)` ＋ `pnpm db:seed --milestones-only`（掃所有 `role='newcomer'` 且有 `start_date` 者，只 insert 缺的 kind）作為安全網，涵蓋 RUNBOOK 5「路 B：Supabase 後台手建 profile」 | 到期日的定義（start_date + 30／60／90）屬業務規則，放 trigger 會讓它離開單元測試覆蓋範圍、且改規則要寫 migration；安全網存在才敢在 Phase 1 允許手建 profile（PLAN 4.7、T29 RUNBOOK 路 B） |
| D-66 | 2026-09-04 | 兩個非框架依賴的用途邊界（PLAN 5 白名單的落地）：`zod` 只用在「外部進來的 JSON／表單值」的解析——`parseQuestions`（`form_versions.questions`）與 `parseRulesSettings`（`settings.rules`），跨欄位的發布驗證仍是手寫純函式（D-18）；`date-fns` ＋ `@date-fns/tz`（`TZDate`）只在 `lib/time` 內部使用，其餘模組一律經 `lib/time` 的函式取台北日期與時刻，`tests/unit/timezone-lint.test.ts` 靜態擋住 `lib/`／`app/` 其他檔案直接用 `getDate()`／`getHours()`／`getDay()`／`toLocaleDateString(` | jsonb 欄位是唯一「型別無法由 TypeScript 保證」的入口，值得一個 schema 函式庫；時區計算若散在各檔，`APP_TIMEZONE` 與假時鐘測試（§11）就失效，故以 lint 測試強制單一出入口（PLAN 5.4、T01、T10） |
| D-67 | 2026-09-04 | 三指標的預警母體在 Phase 1 **只含在職新人**，與 PLAN A02「以 alerts 事實全量計、離職者歷史預警仍計入」不一致：`lib/metrics/rates.ts` 的 `alertPopulation()` 的確不看 `status`（D-33），但 `app/(front)/hr/page.tsx` 把 `listAlertsWithSubmission()` 的 `userIds` 限縮成 `activeNewcomers()`，離職者的預警根本沒被載入。Phase 1 不改程式，先在 `docs/RUNBOOK.md` 4.5／6.4／9.5 據實描述「停用帳號會讓三指標跳動」；Phase 2 隨 `/admin/users` 一併裁決：擴大頁面查詢母體（改 `/hr` 與 `/ceo` 的預警查詢）或正式修訂 A02 | 文件描述行為、不描述意圖：HR 依原本寫法會把停用某人後的指標變動當成系統錯誤來報修。此刻改頁面查詢會同時牽動 `/hr`、`/ceo`、部門統計與待處理預警四處的母體（A02 是它們共用的假設），屬於架構決定而非文件修正，不在 T29 範圍內（T29 審查 major） |
| D-68 | 2026-09-04 | §13「完成定義」在 Phase 1 收尾時分成兩類記錄，**只有第一類算通過**：(一) repo 內可自證的——`pnpm lint`／`pnpm typecheck`／`pnpm test`（`TZ=UTC`／`Asia/Taipei`／`America/Los_Angeles` 各 34 檔 648 測試）／`next build`（13 路由）皆 exit 0；migrations 從空庫一路跑到最新（本容器以本機 PostgreSQL 16 逐檔 `psql -v ON_ERROR_STOP=1` 證明，得九表、`relrowsecurity` 皆 true、`pg_policies` 0 列、8 個 enum）；`.env.example` 涵蓋 grep 得到的全部應用變數；`docs/DECISIONS.md` 已更新。(二) 需要 Docker 或 Supabase staging 金鑰才能成立的——`supabase db reset`、`pnpm db:seed --verify` 兩次一致、Playwright 需登入的 15 條、T28 的 9 張 375px 截圖、30 天 session 真機驗證、staging `/hr` 今日交件應交數 4——一律在 PLAN 第 7 節「Phase 1 交付狀態」列為 blocked 並寫明解除條件，不得在 PR 描述中宣稱已通過 | §13 是 Phase 1 驗收唯一標準，但其中一半的證據不在 repo 裡；把「已證」與「未證」分開列，PR 審查者才能一眼看出還缺什麼，也避免把「測試檔存在」誤當成「測試跑過」 |
| D-69 | 2026-09-04 | `.env.example` 的完整性以 `grep -rho "process\.env\.[A-Z_]*" app lib supabase tests middleware.ts \| sort -u` 為準（T30 驗收欄）。該 grep 另會回報 `NODE_ENV` 與 `CI` 兩個由工具鏈設定的變數（next／vitest 設前者，GitHub Actions 設後者）：兩者以檔尾註解列出並註明「不要在此設值」，**不**給預設值——在 `.env.local` 設 `NODE_ENV` 會破壞 Next.js build。應用自有的七個變數（§4 五個＋`SEED_PASSWORD`、`SEED_ALLOWED_PROJECT_REF`）維持逐行列出 | 若把 `NODE_ENV=production` 之類寫進 `.env.example`，照抄成 `.env.local` 的人會得到一個 dev server 跑 production 產物的壞狀態；但完全不提又會讓下一次 §13 稽核以為漏列 |
